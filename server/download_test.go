package server

import (
	"testing"
)

func TestServer_downloadHandler(t *testing.T) {
	//g := NewGomegaWithT(t)
	//ctrl := gomock.NewController(t)
	//defer ctrl.Finish()
	//config := mocks.NewMockConfig(ctrl)
	//context := rssMocks.NewMockHttpContext(ctrl)
	//
	////Test
	//s := NewServer(config)
	//s.downloadHandler(context)
}
