version: "2"
volumes:
  db:

services:

  torrentd:
    image: sp0x/torrentd:latest
    build: .
    command: watch -v
    env_file:
      - .env
    volumes:
      - db:/db
    ports:
      - 4000:5000
